# bigleaguechew_r3f

Best-effort React Three Fiber reconstruction of the missing `bigleaguechew` demo.

## Notes
- Source assets were copied locally into `public/copied-assets` when available.
- No files are imported from the original `bigleaguechew` directory at runtime.
- The reference screenshot is stored at `public/reference` when available.
