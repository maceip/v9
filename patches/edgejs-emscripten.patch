diff --git a/CMakeLists.txt b/CMakeLists.txt
index 57123bbf..1b7b4f50 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -86,6 +86,12 @@ if(CMAKE_SYSTEM_NAME MATCHES "WASI|WASIX" OR
   set(EDGE_ALLOW_UNDEFINED_IMPORTS ON CACHE BOOL "" FORCE)
 endif()
 
+set(EDGE_IS_EMSCRIPTEN_TARGET OFF)
+if(CMAKE_SYSTEM_NAME STREQUAL "Emscripten")
+  set(EDGE_IS_EMSCRIPTEN_TARGET ON)
+  set(EDGE_ALLOW_UNDEFINED_IMPORTS ON CACHE BOOL "" FORCE)
+endif()
+
 function(edge_detect_native_openssl_target_os out_var)
   if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
     set(_value "linux")
@@ -250,11 +256,15 @@ set(BUILD_MINIZIP_BIN OFF CACHE BOOL "" FORCE)
 set(BUILD_ZPIPE OFF CACHE BOOL "" FORCE)
 set(BUILD_MINIGZIP OFF CACHE BOOL "" FORCE)
 add_subdirectory("${PROJECT_ROOT}/deps/zlib" "${CMAKE_CURRENT_BINARY_DIR}/deps/zlib")
-if(EDGE_IS_WASIX_TARGET AND TARGET zlib_bench)
+if((EDGE_IS_WASIX_TARGET OR EDGE_IS_EMSCRIPTEN_TARGET) AND TARGET zlib_bench)
   # zlib_bench links against the shared zlib target, which fails under WASIX
   # static-linking constraints. Keep the libraries but skip the benchmark tool.
   set_target_properties(zlib_bench PROPERTIES EXCLUDE_FROM_ALL TRUE)
 endif()
+if(EDGE_IS_EMSCRIPTEN_TARGET AND TARGET zlib)
+  # Shared libraries cannot be linked as standalone wasm (no main).
+  set_target_properties(zlib PROPERTIES EXCLUDE_FROM_ALL TRUE)
+endif()
 
 find_package(Threads REQUIRED)
 
@@ -391,7 +401,22 @@ target_compile_definitions(edge_nghttp2
     _U_=
 )
 
-if(EDGE_IS_WASIX_TARGET)
+if(EDGE_IS_EMSCRIPTEN_TARGET)
+  # Emscripten: OpenSSL crypto is not available in the browser.
+  # Create stub imported targets so downstream target_link_libraries() calls resolve.
+  # The actual crypto operations must be shimmed via JS at runtime.
+  set(_edge_openssl_include_dir "${PROJECT_ROOT}/deps/openssl/openssl/include")
+  add_library(OpenSSL::Crypto INTERFACE IMPORTED GLOBAL)
+  set_target_properties(OpenSSL::Crypto PROPERTIES
+    INTERFACE_INCLUDE_DIRECTORIES "${_edge_openssl_include_dir}"
+    INTERFACE_COMPILE_DEFINITIONS "OPENSSL_NO_ASM=1"
+  )
+  add_library(OpenSSL::SSL INTERFACE IMPORTED GLOBAL)
+  set_target_properties(OpenSSL::SSL PROPERTIES
+    INTERFACE_INCLUDE_DIRECTORIES "${_edge_openssl_include_dir}"
+  )
+  target_link_libraries(OpenSSL::SSL INTERFACE OpenSSL::Crypto)
+elseif(EDGE_IS_WASIX_TARGET)
   set(EDGE_OPENSSL_WASIX_ROOT
     "${PROJECT_ROOT}/deps/openssl-wasix"
     CACHE PATH "Path to wasix-org OpenSSL source/build tree"
@@ -1019,7 +1044,7 @@ if(EDGE_BUILD_CLI)
     )
   endif()
 
-  if(NOT EDGE_IS_WASIX_TARGET)
+  if(NOT EDGE_IS_WASIX_TARGET AND NOT EDGE_IS_EMSCRIPTEN_TARGET)
     add_executable(edgeenv
       "${EDGE_ROOT}/src/edgeenv_main.cc"
     )
@@ -1030,7 +1055,7 @@ if(EDGE_BUILD_CLI)
   endif()
 endif()
 
-if(NOT EDGE_IS_WASIX_TARGET)
+if(NOT EDGE_IS_WASIX_TARGET AND NOT EDGE_IS_EMSCRIPTEN_TARGET)
   enable_testing()
   add_subdirectory(tests)
 endif()
diff --git a/deps/uv/src/unix/pipe.c b/deps/uv/src/unix/pipe.c
index 68e225e2..1ec6a34f 100644
--- a/deps/uv/src/unix/pipe.c
+++ b/deps/uv/src/unix/pipe.c
@@ -498,7 +498,7 @@ int uv_pipe_chmod(uv_pipe_t* handle, int mode) {
 int uv_pipe(uv_os_fd_t fds[2], int read_flags, int write_flags) {
   uv_os_fd_t temp[2];
   int err;
-#if defined(__linux__) || \
+#if (defined(__linux__) && !defined(__EMSCRIPTEN__)) || \
     defined(__FreeBSD__) || \
     defined(__OpenBSD__) || \
     defined(__DragonFly__) || \
diff --git a/deps/uv/src/unix/thread.c b/deps/uv/src/unix/thread.c
index 34fea364..9c6fdd56 100644
--- a/deps/uv/src/unix/thread.c
+++ b/deps/uv/src/unix/thread.c
@@ -897,7 +897,7 @@ void uv_key_set(uv_key_t* key, void* value) {
     abort();
 }
 
-#if defined(_AIX) || defined(__MVS__) || defined(__PASE__)
+#if defined(_AIX) || defined(__MVS__) || defined(__PASE__) || defined(__EMSCRIPTEN__)
 int uv__thread_setname(const char* name) {
   return UV_ENOSYS;
 }
@@ -938,7 +938,8 @@ int uv__thread_setname(const char* name) {
 #if (defined(__ANDROID_API__) && __ANDROID_API__ < 26) || \
     defined(_AIX) || \
     defined(__MVS__) || \
-    defined(__PASE__)
+    defined(__PASE__) || \
+    defined(__EMSCRIPTEN__)
 int uv__thread_getname(uv_thread_t* tid, char* name, size_t size) {
   return UV_ENOSYS;
 }
diff --git a/src/edge_runtime.cc b/src/edge_runtime.cc
index 7afcc7a3..d5b501a8 100644
--- a/src/edge_runtime.cc
+++ b/src/edge_runtime.cc
@@ -3570,6 +3570,12 @@ int EdgeRunWorkerThreadMain(napi_env env,
 }
 
 bool EdgeInitializeOpenSslForCli(std::string* error_out) {
+#ifdef __EMSCRIPTEN__
+  // Emscripten: OpenSSL is stubbed — crypto operations are shimmed via JS
+  // (WebCrypto API). Skip native OpenSSL initialization entirely to prevent
+  // abort() from null CSPRNG provider.
+  return true;
+#else
   const edge_options::EffectiveCliState state = edge_options::BuildEffectiveCliState(g_edge_cli_exec_argv);
   const std::vector<std::string>& exec_argv = state.ok ? state.effective_tokens : g_edge_cli_exec_argv;
   if (!ConfigureOpenSslFromExecArgv(exec_argv, error_out)) {
@@ -3581,6 +3587,7 @@ bool EdgeInitializeOpenSslForCli(std::string* error_out) {
     std::abort();
   }
   return true;
+#endif
 }
 
 void EdgeSetCurrentScriptPath(const std::string& script_path) {
